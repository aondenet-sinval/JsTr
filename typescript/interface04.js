var nomesFemininos;
nomesFemininos = ['Ana', 'Maria', 'Renata'];
var nomes = nomesFemininos[0];
console.log(nomes); //Ana
//Percorrendo o array
var indice = 0;
while (indice < 3) {
    var percorrerNomes = nomesFemininos[indice];
    console.log(percorrerNomes); //Ana Maria Renata
    ++indice;
}
;
