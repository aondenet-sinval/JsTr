//métodos:
//toString()
let lista = ['lápis', 'borracha', 'caneta'];
lista = lista.toString();//Decomosição do array em string
for (var i = 0; i < lista.length; i++) {
  console.log(lista[i]);//Percorrendo cada elemento da string
};
